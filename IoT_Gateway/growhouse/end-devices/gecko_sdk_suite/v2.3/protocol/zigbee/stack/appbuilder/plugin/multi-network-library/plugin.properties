name=Multi-network Library 
description=The Multi-network Library provides the ability for a single device to be on multiple networks and time-sharing between them.  The device can be a router on only one network, but an end device on another network.
category=Stack Libraries

qualityString=Production Ready
quality=production

includedByDefault=false
requiredChipFeature=multi-network

providedApis=multi-network

libraryFiles=$FRAMEWORK/../../build/multi-network-library-{platformDirectorySuffix}/multi-network-library.a
libraryStubFiles=$FRAMEWORK/../../build/multi-network-stub-library-{platformDirectorySuffix}/multi-network-stub-library.a
